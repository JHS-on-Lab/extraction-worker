# DB
SQLAlchemy==2.0.41
PyMySQL==1.1.1

# SSH Tunnel
sshtunnel==0.4.0
paramiko==3.5.1

# Config
python-dotenv==1.1.0

# HTTP / Headless
httpx==0.28.1
certifi==2026.06.17
# 반드시 Dockerfile의 베이스 이미지 태그(mcr.microsoft.com/playwright/python:vX.Y.Z)와
# 동일 버전으로 맞출 것. 어긋나면 pip 패키지와 이미지에 내장된 브라우저/드라이버 버전이
# 안 맞아 headless fetch 가 전부 실패한다 (에러 메시지가 asyncio 관련으로 나와 헷갈리기 쉬움).
# TODO: 이 불일치를 빌드 타임에 잡을 수 있도록 스모크 테스트(브라우저 기동+빈 페이지 로드) 추가 검토.
playwright==1.59.0

# Extraction
trafilatura==2.0.0
readability-lxml==0.8.4.1
lxml==5.4.0
selectolax==0.3.28

# Dev / Test
pytest==8.4.0
pytest-asyncio==1.0.0
